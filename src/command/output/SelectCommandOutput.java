package command.output;

public class SelectCommandOutput extends CommandOutput {
    public SelectCommandOutput(String user, Integer timestamp, String message) {
        super(user, timestamp, message);
    }
}
